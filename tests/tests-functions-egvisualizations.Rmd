---
title: "test plots"
author: '-'
date: "July 7, 2018"
output: html_document
---
```{r}
library(wordcloud)
library(ggplot2)
library(egvisualizations)
library(dplyr)

data <- readRDS("/home/pablo/Desktop/ethergeist-final/Argentina-2018-08-09.Rds")

```

```{r}
egbarplotcount(data$news, "media_name", "..count..", "media_name","a","b", "test")
```

```{r}
a <- data$news_wordcloud %>% group_by(word) %>% count()
egwordcloud(a$word, a$n)
```


```{r}
egdataseries(data$tweets, "created_at", "screen_name","Date","Count","Candidate")

egdataseries(data$youtube_videos, "publication_date", NA,"Date","Count","Candidate")
```

```{r}
a <- egdataanalysis::network_structure_relation_mentions(data$entities, 
                                                         data$news_entities_mentions)
test <- a[[2]] %>% left_join(a[[1]] %>% select(id, title) %>% rename(totitle = title), by = c("to"="id")) %>% left_join(a[[1]] %>% select(id, title) %>% rename(fromtitle = title), by = c("from"="id"))

egvisualizations::egheatmap(test, "totitle","fromtitle","length")
```
